# 작은 도서관 관리 시스템

C 언어와 SQLite로 구현한 도서관 도서 대출/반납 및 회원 관리 시스템입니다.

## 기술 스택

- **언어**: C
- **데이터베이스**: SQLite3
- **컴파일러**: GCC (MinGW/MSYS2)

## 주요 기능

### 📚 도서 관리
- 도서 등록, 검색, 수정, 삭제
- 전체 도서 목록 조회
- 장르별/저자별 검색
- 도서 재고 관리

### 👥 회원 관리
- 회원 등록, 정보 수정, 삭제
- 회원 검색 (이름/ID)
- 연체 상태 확인
- 전체 회원 목록

### 📖 대출/반납 관리
- 도서 대출 처리 (기본 14일)
- 도서 반납 처리
- 연체 관리 (연체 1일당 2일씩 대출 정지)
- 회원별/도서별 대출 이력 조회
- 활성 대출 목록
- 연체 도서 목록

### 📊 보고서
- 인기 도서 Top 10
- 연체 현황 보고서
- 도서 재고 현황
- 회원 통계

## 빌드 방법

### Windows (PowerShell)

**방법 1: PowerShell 스크립트 사용**
```powershell
# 빌드
.\build.ps1

# 빌드 후 실행
.\build.ps1 run

# 클린 빌드
.\build.ps1 rebuild

# 정리
.\build.ps1 clean
```

**방법 2: mingw32-make 직접 사용**
```powershell
C:\msys64\ucrt64\bin\mingw32-make.exe all
```

### Linux/Mac

```bash
make
```

## 실행 방법

빌드 후 실행:
```powershell
.\bin\library.exe
```

또는:
```powershell
.\build.ps1 run
```

## 프로젝트 구조

```
task03/
├── include/           # 헤더 파일
│   ├── book.h
│   ├── member.h
│   ├── loan.h
│   └── database.h
├── src/              # 소스 파일
│   ├── main.c
│   ├── book.c
│   ├── member.c
│   ├── loan.c
│   └── database.c
├── obj/              # 오브젝트 파일 (자동 생성)
├── bin/              # 실행 파일 (자동 생성)
├── database/         # 데이터베이스 파일 (자동 생성)
├── Makefile          # 빌드 설정
├── build.ps1         # PowerShell 빌드 스크립트
└── README.md
```

## 의존성

### Windows (MSYS2)
```bash
pacman -S mingw-w64-ucrt-x86_64-gcc
pacman -S mingw-w64-ucrt-x86_64-sqlite3
pacman -S mingw-w64-ucrt-x86_64-make
```

### Ubuntu/Debian
```bash
sudo apt-get install gcc
sudo apt-get install libsqlite3-dev
sudo apt-get install make
```

### macOS
```bash
brew install gcc
brew install sqlite3
```

## 데이터베이스 스키마

### Books (도서)
- `book_id`: 도서 ID (PK)
- `title`: 제목
- `author`: 저자
- `publisher`: 출판사
- `publication_year`: 출판년도
- `isbn`: ISBN
- `genre`: 장르
- `quantity`: 총 수량
- `available`: 대출 가능 수량

### Members (회원)
- `member_id`: 회원 ID (PK)
- `name`: 이름
- `phone`: 전화번호
- `address`: 주소
- `registration_date`: 등록일

### Loans (대출)
- `loan_id`: 대출 ID (PK)
- `book_id`: 도서 ID (FK)
- `member_id`: 회원 ID (FK)
- `loan_date`: 대출일
- `due_date`: 반납 예정일
- `is_returned`: 반납 여부

### Returns (반납)
- `return_id`: 반납 ID (PK)
- `loan_id`: 대출 ID (FK)
- `return_date`: 반납일
- `overdue_days`: 연체 일수

## 연체 관리 규칙

- 연체 시 **연체 일수 × 2일** 동안 대출 정지
- 예: 3일 연체 → 6일간 대출 불가

## 개발자

- 코딩 스타일: C 표준 (snake_case)
- 메모리 관리: SQLite3 API 적절히 사용
- 에러 처리: 모든 함수에서 에러 코드 반환

## 라이센스

MIT License
